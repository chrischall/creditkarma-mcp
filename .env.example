CK_COOKIES=   # Run `npm run auth -- .env` to capture via browser login (or copy CKAT from DevTools).
CK_DB_PATH=   # Optional: custom path for SQLite DB (default: ~/.creditkarma-mcp/transactions.db)
