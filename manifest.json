{
  "$schema": "https://raw.githubusercontent.com/anthropics/dxt/main/dist/mcpb-manifest.schema.json",
  "manifest_version": "0.3",
  "name": "creditkarma-mcp",
  "display_name": "Credit Karma",
  "version": "2.2.3",
  "description": "Credit Karma transaction tracking for Claude — sync and query your transactions, spending by category and merchant, and account summaries via natural language",
  "author": {
    "name": "Chris Chall",
    "email": "chris.c.hall@gmail.com",
    "url": "https://github.com/chrischall"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/chrischall/creditkarma-mcp"
  },
  "homepage": "https://github.com/chrischall/creditkarma-mcp",
  "support": "https://github.com/chrischall/creditkarma-mcp/issues",
  "license": "MIT",
  "keywords": [
    "creditkarma",
    "transactions",
    "finance",
    "spending",
    "accounts"
  ],
  "server": {
    "type": "node",
    "entry_point": "dist/bundle.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/dist/bundle.js"
      ],
      "env": {
        "CK_COOKIES": "${user_config.ck_cookies}",
        "CK_DB_PATH": "${user_config.ck_db_path}"
      }
    }
  },
  "user_config": {
    "ck_cookies": {
      "type": "string",
      "title": "Credit Karma Cookie Header",
      "description": "Optional. Leave blank to use the fetchproxy 0.3.0 extension fallback — sign into creditkarma.com in the browser and the MCP will read the cookies automatically. Otherwise paste the full Cookie header from a signed-in creditkarma.com request (DevTools → Network → any creditkarma.com request → Request Headers).",
      "required": false,
      "sensitive": true
    },
    "ck_db_path": {
      "type": "string",
      "title": "Database File Path",
      "description": "Optional override for the SQLite database file. Defaults to ~/.creditkarma-mcp/transactions.db. Set to a custom absolute path if you want the data stored elsewhere.",
      "required": false
    }
  },
  "tools": [
    {
      "name": "ck_set_session",
      "description": "Store a Credit Karma session cookie to enable automatic token refresh"
    },
    {
      "name": "ck_sync_transactions",
      "description": "Sync Credit Karma transactions into the local SQLite database"
    },
    {
      "name": "ck_list_transactions",
      "description": "List transactions with optional filters. Paginated."
    },
    {
      "name": "ck_get_recent_transactions",
      "description": "Return the N most recent transactions"
    },
    {
      "name": "ck_get_spending_by_category",
      "description": "Group debit transactions by category and return totals"
    },
    {
      "name": "ck_get_spending_by_merchant",
      "description": "Return top merchants by total debit spend"
    },
    {
      "name": "ck_get_account_summary",
      "description": "Return per-account debit, credit, and net totals"
    },
    {
      "name": "ck_query_sql",
      "description": "Execute a raw SQL SELECT query against the transactions database"
    }
  ],
  "compatibility": {
    "platforms": [
      "darwin",
      "win32",
      "linux"
    ],
    "runtimes": {
      "node": ">=22.5.0"
    }
  }
}
